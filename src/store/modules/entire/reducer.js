// redux6
const initialState = {
  currentPage: 3

}

function reducer(state=initialState,action) {
  switch(action.type) {
    // case

    // 先加这个
    default:
      return state
  }
}

export default reducer